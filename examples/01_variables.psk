// 変数と定数のサンプル

// letは再代入可能な変数を宣言します
let message = "Hello, Pisuke!"

// constは変更不可能な定数を宣言します
const GREETING = "Hello, World!"

// 現在、変数の値を出力する標準的な方法がないため、
// このコードはコンパイルはできますが、実行しても何も表示されません。
// 今後の `print()` 関数の実装にご期待ください！
