// 関数のサンプル

// 二つの数値を足す関数を定義します
let add = fn(a, b) {
  return a + b
}

// `let`で定義した関数は、他の変数と同じように扱えます。
const five = 5
let ten = 10

let result = add(five, ten)

// `result` には 15 が格納されますが、
// 現在はまだ `print` 機能がないため、コンパイルは通るものの
// 実行しても結果は表示されません。
