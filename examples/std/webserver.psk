// std/webserver.psk
// Minimal std webserver module used by examples

let server = {
    serve: fn(port) {
        // placeholder: real server behavior is generated by codegen when calling server.serve
    },
    static: fn(path) {
        // placeholder for static file serving
    },
    route: fn(path, handler) {
        // placeholder for registering a route - codegen handles server.route specially
    }
}
